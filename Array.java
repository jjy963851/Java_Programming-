int[] a = new int[10]; size ten. length ten. Index 9

a[0] = 1
a[1] = 2
q[2] = 3

for(int index = 0;// your loop always start from index o 
     i<a.length// which mean do this loop until length -1. 
     i++
     { a[index] = index + 1;
         currentszie++//we have to have currentize for tracking how many values we have
        }
        (for int i = 0; i<currentsize; i++) == samething. 
        a[i] = jy.nextInt
        a[1] = userInput
        a[2] = userInput
        a[3] = userInput 
        [] is the index 
        
        for(int value : a(array)) == I will get the value from a
        {
            sout(value)
        }
        
        for( in row = 0; row< a.length; row++)
        {
            for(in col = 0; col<a[0].length;(//this one indicate collumm length) col++)
            {sout(a[row][col])
            }
        }
        
        ArrayList<intergers  > myArrayList = new Arraylist<>();
        myarraylist add (1);
        {0,5,3,8,20,1}
         0 1 2 3 4 5
        double largest = values[0]; // this is how you find the largest number in array
        for (int i = 1; i < values.length; i++)
        {
            if (values[i] > largest)
            {
                largest = values[i];
            }
        }
        
        
        ArrayList<double> inputs = new ArrayList<double>();//in array list we dont use currentsize. Array list automatically increase the size by userinput
        
        